{
  "name": "earth-movers-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply earth-movers-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply earth-movers-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.29.0"
  }
}
